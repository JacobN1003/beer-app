{
  "name": "beer-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^5.3.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.6"
  },
  "scripts": {
    "start": "concurrently \"nodemon server.js\" \"npm run client\"",
    "client": "cd client && yarn start"
  }
}
